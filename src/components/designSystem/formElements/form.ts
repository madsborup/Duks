import styled from "styled-components";
import base from "../base";

export const StyledForm = styled.form`
  flex: 1 0 auto;
  display: flex;
  flex-direction: column;
`;

export const TwoColumnForm = styled